# Xcode project is generated by XcodeGen — run `xcodegen generate` after cloning
*.xcodeproj/
*.xcworkspace/
Generated/

# Build products
build/
DerivedData/
*.hmap

# Swift Package Manager
.swiftpm/
.build/

# User-specific
xcuserdata/
*.xcuserstate

# macOS
.DS_Store

# CocoaPods / Carthage (unused today, ignored defensively)
Pods/
Carthage/Build/

# Supabase CLI local state (project ref cache, no secrets — but not repo content)
supabase/.temp/

# Documentation Stripe rapatriée automatiquement par l'outillage : ce ne sont
# pas nos fichiers, ils se retéléchargent, et ils noyaient « git status ».
.agents/
.claude/skills/
skills-lock.json
